bool isPalindrome(int x) {
    
    int temp;
    long y = 0;

    temp = x;

    if (x < 0 )
    {
        return false;
    }
    while (x != 0) {
        y = y * 10 + x % 10;
        x /= 10;
    }

    if (temp == y)
    {
        return true;
    }
    else
    {
        return false;
    }
}
